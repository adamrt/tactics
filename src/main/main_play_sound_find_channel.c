extern void SuzukiPlaySoundFindChannel(int sound_id);

void main_play_sound_find_channel(int sound_id) {
    SuzukiPlaySoundFindChannel(sound_id);
}

/* Keep the preprocessed source stable for the legacy compiler under QEMU. */
